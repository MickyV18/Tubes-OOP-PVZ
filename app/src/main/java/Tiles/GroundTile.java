package Tiles;

import Creature.Plant.Plant;
import Sun.Sun;

public class GroundTile extends Tile {

    public GroundTile() {
        super();
    }

    // @Override
    // public void addPlant(Plant plant) {
    //     if (Sun.getSun() >= plant.getCost())
    //     {
    //         if (this.getPlant() == null && !plant.isAquatic()) {
    //             this.addPlant(plant);
    //             Sun.decreaseSun(plant.getCost());
    //         }
    //     }
    // }
}